# Database
DATABASE_URL=postgresql://admin:admin@localhost:5433/portfolio_db

# JWT — replace with a strong random string in production
SECRET_KEY=change-this-to-a-long-random-secret-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Debug — set to true to log SQL queries
SQL_ECHO=false
